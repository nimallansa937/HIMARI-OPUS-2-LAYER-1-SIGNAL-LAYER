# HIMARI L1 Signal Layer Dependencies
# Add to existing requirements.txt or install separately

# Core (likely already installed)
numpy>=1.24.0
scipy>=1.11.0
pandas>=2.0.0
redis>=5.0.0

# Layer 1 Enhancements (NEW)
talipp>=2.0.0              # O(1) streaming indicators
vaderSentiment>=3.3.2      # Lexicon-based sentiment (MANDATORY)
transformers>=4.30.0       # FinBERT transformer (MANDATORY)
torch>=2.0.0               # PyTorch for transformers (MANDATORY)

# Streaming algorithms
tdigest>=0.5.2

# ML/Statistics
scikit-learn>=1.3.0
arch>=6.0.0              # GARCH, White/SPA tests
hmmlearn>=0.3.0          # Hidden Markov Models
boruta>=0.3              # Feature selection

# Validation
mlfinlab>=1.5.0          # CPCV, DSR (optional - can implement manually)

# Performance optimization
numba>=0.58.0            # JIT compilation for hot paths

# Testing
pytest>=7.4.0
pytest-benchmark>=4.0.0

# Kafka consumer (likely already installed)
kafka-python>=2.0.2
confluent-kafka>=2.3.0   # Alternative, faster

# Monitoring
prometheus-client>=0.19.0
